# tailscale-preview-site — 設定範例
#
# 複製這份到 ~/.config/previews/config.env 再改成你的值。
# 這個檔案裡是「你這台機器的資訊」，不要放進版本控制。

# 網頁放哪個資料夾（站台就是把這個資料夾整個開出去）
PREVIEWS_DIR="$HOME/previews"

# 綁哪個 IP —— 一定要填你的 Tailscale IP（100.x.y.z）
#
# 怎麼查：tailscale ip -4
#
# 為什麼不能填 0.0.0.0：那會綁到這台機器的所有網路介面，
# 包含你家或辦公室的區網。同一個 Wi-Fi 下的任何裝置都連得進來。
PREVIEWS_BIND_IP="100.0.0.0"

PREVIEWS_PORT="8787"

# 只允許在這台機器上發佈（比對 hostname 前綴）。留空＝不檢查。
# 用途：避免你在筆電上跑發佈指令，結果檔案進了筆電的資料夾、站台卻在另一台。
PREVIEWS_HOSTNAME_PREFIX=""

# 首頁標題
PREVIEWS_TITLE="預覽清單"

# 選配：發佈前檢查文件裡有沒有「這份要讀的人決定什麼」的段落，抓不到就提醒。
# 留空＝關閉。填成 grep -E 吃得下的樣式即可。
PREVIEWS_DECISION_REGEX=""
